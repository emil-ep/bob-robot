apiVersion: v1
kind: Service
metadata:
  name: inventory-service-preview
spec:
  selector:
    app: inventory-service
  ports:
    - protocol: TCP
      port: 80
      targetPort: 3001
  type: ClusterIP

# Made with Bob
